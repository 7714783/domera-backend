// INIT-013 — Team is the people-domain catalogue. The single source of
// "who can be assigned to a task / approve a doc / receive an alert".

import { MODULE_CATEGORIES } from '../../common/module-categories';

export const MODULE_CATEGORY = 'people' as (typeof MODULE_CATEGORIES)[number];
