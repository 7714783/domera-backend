// INIT-013 — module category. The per-workspace contractor link table —
// belongs to the people/enterprise domain.

import { MODULE_CATEGORIES } from '../../common/module-categories';

export const MODULE_CATEGORY = 'enterprise' as (typeof MODULE_CATEGORIES)[number];
